# Database
DATABASE_URL=sqlite:///./data/calendar.db

# Security
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3333,http://frontend:3000

# Environment
ENVIRONMENT=development

# E-Mail (Microsoft 365 Business / SMTP)
# App-Passwort unter https://account.microsoft.com/security erstellen
SMTP_ENABLED=false
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=kalender@ihre-domain.de
SMTP_PASSWORD=ihr-app-passwort
SMTP_FROM_EMAIL=kalender@ihre-domain.de
SMTP_FROM_NAME=JuLis Kalender
APP_URL=http://localhost:3000
