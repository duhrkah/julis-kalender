# Backend Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
CORS_ORIGINS=http://localhost:3000,http://frontend:3000
ENVIRONMENT=production

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_APP_URL=http://localhost:3000

# E-Mail (Microsoft 365 Business / SMTP)
# Bei Genehmigung/Ablehnung von Events wird der Einreicher per E-Mail benachrichtigt.
# Für Microsoft 365: App-Passwort unter https://account.microsoft.com/security erstellen.
SMTP_ENABLED=false
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=kalender@ihre-domain.de
SMTP_PASSWORD=ihr-app-passwort
SMTP_FROM_EMAIL=kalender@ihre-domain.de
SMTP_FROM_NAME=JuLis Kalender
APP_URL=https://kalender.jlssrv.de

# Produktion (Nginx + Certbot)
DOMAIN=kalender.jlssrv.de
CERTBOT_EMAIL=admin@example.com
